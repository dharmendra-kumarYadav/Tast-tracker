#!/usr/bin/env sh
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
JAR="$SCRIPT_DIR/target/task-cli.jar"
if [ ! -f "$JAR" ]; then
  echo "task-cli.jar not found. Run mvn package first." >&2
  exit 1
fi
exec java -jar "$JAR" "$@"
